apiVersion: v1
kind: Service
metadata:
  name: release-name-activate2-fe
  labels:
    helm.sh/chart: activate2-fe-0.1.0
    app: release-name-activate2-fe
    app.kubernetes.io/name: activate2-fe
    app.kubernetes.io/instance: release-name
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: activate2-fe
    app.kubernetes.io/instance: release-name